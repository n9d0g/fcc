import { Layout } from '../../components/util/Layout'

const Give = () => {
  return (
    <Layout title="FCC | Give">
      <section>
        <h1 className="text-5xl">Give</h1>
      </section>
    </Layout>
  )
}

export default Give
