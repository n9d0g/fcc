{
	"name": "fcc",
	"version": "3.11.3",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"format": "prettier --write .",
		"clean": "rm -rf node_modules package-lock.json apps/*/node_modules apps/*/package-lock.json apps/*/.svelte-kit apps/*/.turbo",
		"fresh": "npm run clean && npm i --legacy-peer-deps && npm run format",
		"fresh:start": "npm run fresh && npm run dev"
	},
	"devDependencies": {
		"@semantic-release/git": "^10.0.1",
		"@semantic-release/github": "^9.0.4",
		"@semantic-release/npm": "^10.0.5",
		"eslint": "^8.48.0",
		"prettier": "^3.0.3",
		"prettier-plugin-svelte": "^3.0.3",
		"semantic-release": "^21.0.9",
		"tsconfig": "*",
		"turbo": "latest"
	},
	"packageManager": "npm@9.5.0",
	"workspaces": [
		"apps/*"
	]
}
